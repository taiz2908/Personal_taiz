Ref:
 - https://www.youtube.com/watch?v=BLQ0PDjgN8w
 - https://www.jenkins.io/doc/book/pipeline/shared-libraries/

Project:
 - https://www.youtube.com/watch?v=g-v9AsubOqY Complete Real-time DevOps CI/CD Project | Jenkins Shared Lib | DevSecOps | Advanced Pro | HandsOn

======================
 - Common reusable code. 
 - store in one place
 - in Jenkinsfile we call the function in the name of shared-libraries and accordingly it'll executes.
 - written in .groovy extension.  

Advantages
 - Standarization of Pipelines
 - Reduce duplication of code
 - Easy onboarding of new applications, projects or teams
 - One place to fix issues with the shared or common code
 - Code Maintainence
 - Reduce the risk of errors

Shared libraries divided into three folders. 
 1. vars
 2. src 
 3. resources 

How to Configure in Jenkins?
 - Manage Jenkins -> Configure systems -> Global-pipeline libraries
 - Need to provide the name, branch name and git repo url. 
   - Name: Same as library_name define in the pipeline. 
   - Rtrieval method: Modern SCM
   - Need to provide the shared library repo_link
-------------------------------------------------------------
Ex: helloWorld.groovy
def call() {
  sh 'Hello World'
}

Jenkinsfile:
@Library("library_name") _ 
pipeline {
  agent any
  stages {
    stage('hello') {
      steps {
        helloWorld()
      }
    }
  }
}
