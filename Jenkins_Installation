https://www.jenkins.io/doc/developer/plugin-development/dependency-management/

==============: INSTALLATION SCRIPTS :=================================
Ref
- https://www.jenkins.io/doc/book/installing/linux/#red-hat-centos

#yum -y update
#sudo yum install java-11-openjdk or yum install java* -y 
or,
amazon-linux-extras list
amazon-linux-extras install java-openjdk11

#sudo wget -O /etc/yum.repos.d/jenkins.repo \
    https://pkg.jenkins.io/redhat-stable/jenkins.repo
#sudo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io.key
#sudo yum upgrade

#sudo yum install jenkins
#sudo systemctl daemon-reload

#sudo systemctl enable jenkins
#sudo systemctl start jenkins
#sudo systemctl status jenkins
#sudo cat /var/lib/jenkins/secrets/initialAdminPassword

#find /usr/lib/jvm/java-11-openjdk-11* | head -n 3
#vi .bash_profile 
 JAVA_HOME=/usr/lib/jvm/java-11-openjdk-11.0.13.0.8-1.amzn2.0.3.x86_64
 PATH=$JAVA_HOME/bin:$PATH
#echo $JAVA_HOME
 /usr/lib/jvm/java-11-openjdk-11.0.13.0.8-1.amzn2.0.3.x86_64
#source ~/.bash_profile
==================================
Note: 
 - By default, Jenkins stores all of its data in this directory on the file system: /var/lib/jenkins
Examples:
 InitilaAdminPassword: /var/lib/jenkins/secrets/initialAdminPassword
 Logs: cd /var/lib/jenkins/logs
 Jobs: cd /var/lib/jenkins/jobs/
---
Jenkins configuration file in the following directory: /etc/default/Jenkins
#systemctl edit jenkins
 - paste it under it. Environment="JENKINS_LOG=%L/jenkins/jenkins.log"
#systemctl restart jenkins

--- Contents inside /var/lib/jenkins  ------------------
root@ip-172-31-92-133 ~]# cd /var/lib/jenkins 

%C                                                                       jobs
caches                                                                   logs
config.xml                                                               nodeMonitors.xml
hudson.model.UpdateCenter.xml                                            org.jenkinsci.plugins.gitclient.JGitApacheTool.xml
hudson.plugins.emailext.ExtendedEmailPublisher.xml                       org.jenkinsci.plugins.gitclient.JGitTool.xml
hudson.plugins.git.GitTool.xml                                           org.jenkinsci.plugins.resourcedisposer.AsyncResourceDisposer.xml
hudson.plugins.gradle.Gradle.xml                                         org.jenkinsci.plugins.workflow.flow.FlowExecutionList.xml
hudson.tasks.Ant.xml                                                     plugins
hudson.tasks.Maven.xml                                                   queue.xml
identity.key.enc                                                         queue.xml.bak
io.jenkins.plugins.pipelinegraphview.PipelineGraphViewConfiguration.xml  secret.key
io.jenkins.plugins.thememanager.ThemeManagerPageDecorator.xml            secret.key.not-so-secret
jenkins.console.ConsoleUrlProviderGlobalConfiguration.xml                secrets
jenkins.install.InstallUtil.lastExecVersion                              updates
jenkins.install.UpgradeWizard.state                                      userContent
jenkins.mvn.GlobalMavenConfig.xml                                        users
jenkins.telemetry.Correlator.xml                                         workspace

=================: JENKINS BK & UPGRADE STEPS :====================
- URL: https://www.jenkins.io/doc/upgrade-guide/

Method_1: 
 - Jenkins Full Backup & restore: "https://www.youtube.com/watch?v=gIUfI6cNZTo&t=1413s"
 - tar -zcvf jenkins.bk.tar.gz /var/lib/jenkins/
 - aws s3 cp <cloud> .
 - rm -rf /var/lib/jenkins/*
 - tar -zxvf jenkins.bk.tar.gz -C /
 - systemctl start jenkins

Method_2:
 - Upgrade Jenkins using .war file "https://www.youtube.com/watch?v=uNeIJLJpYxw"
 - cd /usr/share/java/jenkins.war
 - check the permission 
 - Apply the same permission with the new .war file.
 - chmod 644 jenkins.war
 - chown root:root jenkins.war
 - Jenkins_Server: "Manage Jenkins" -> "System Information" -> executable-war
 
------------------------- Build Trigger's ---------------------------------------
- Builds Periodically triggers builds as per the schedule (every 10 minutes) even if you haven't changed anything.
- Poll SCM will check for changes before triggering any build, if there are changes to the previous version there only build will be triggered.

1. Build Periodically : cron job / 5 astrics 
2. Poll scm : * * * * * 
3. Build after other projects are Build
4. Trigger build remotely
5. Manual 
6. github hook trigger for git scm polling. 

------------------------- Jenkinsfile - Pipeline as a code-----------------------------------
Ex:
pipeline {
    agent any
    stages {
        stage('Cloning the Repo') {
            steps {
                echo "Cloning the repo.."
            }
        }
    }
}

 1. Scripted & Declarative - Both uses groovy , Here 1st uses syntax
 2. Declarative: Starts with pipeline 

Required Arrtibutes:
1) pipeline
2) agent any - where to execute
3) stages - need to define different different stage
4) stage - Whole work happens 'checking out the code/clone/build/test/deploy'
5) steps - where scripts written

6) "post" -
 - Executes some logic after all 'stages' completed
 - conditions/types: "cleanup / always / success / failure" etc.

7) Define Conditionals for each stage: 
 - Only want to run the "build / test" in Dev Branch but not in other branch. 
 - So when we can define ("when" with "expression"), Step i.e hello which can execute if the branch is 'DEV'
 - Note: Using env variable for the branch i.e env.BRANCH_NAME / BRANCH.NAME , CODE_CHANGES

8)Parameters: 
 - Apply to all the branches 
 - External configuration that you want to your build 
 - we want to be select which version to be deployed so we define by using paramters.
 - Various Types of Parameters: (string , text, booleanParam, choice, password) etc..
 - If you want to genereate the parameters script then choose ,
 - pipeline syntax->properties->this project is parameterised

9)Parallel Build: 
 - In build if we have many steps in a single pipeline it will take more time to complete the build.
 - for this we run the build in parallel inside the declarative pipeline.
 - Blue Ocean Plugin: We can see the build visually .

=========================================================================================================


-------------How to add / provide "Admin / any other access" to user's:--------------------------------

- We need to go to: Manage jenkins--> security-->configure global security-->
  - Matrix-based security
  - project-based matrix authorization strategy 
  - Role-based strategy
------------------------------------------------------------------------------
JNLP Port in jenkins ?

=========: GIT_PRIVATE_REPO :=========
Two Auth:
 1. HTTPS
 2. SSH

1. HTTPS: Best Option (username & type password)
  - GITHUB: Settings -> Developer settings -> Personal access token -> Generate token -> Repo(all), read.org, user:email -> Generate toekn
  - JENKINS: Credentials: -> Add:Jenkins -> Username(email address) -> tik:treat username as secrete -> paste token -> id(choose yours)

2. SSH:
  -  GITHUB: Settings -> Developer settings -> SSH & GPG KEYS -> Need to generate keys in linux
